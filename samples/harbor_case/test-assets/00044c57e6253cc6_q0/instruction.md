The SensitiveDataSanitizer at /app/source/my-gateway/src/main/java/com/leoli/gateway/filter/accesslog/sanitizer/SensitiveDataSanitizer.java recursively masks sensitive fields in nested JSON objects via the maskObjectNode method, but it does not recurse into JSON arrays. A request body like {"users": [{"password": "secret123", "name": "Alice"}, {"password": "hunter2", "name": "Bob"}]} will leave the password fields inside the array unmasked, because the forEach only checks value.isObject() and never handles value.isArray(). Add array recursion to the sanitizer so that all objects nested at any depth within arrays also have their sensitive fields masked. The existing behavior for top-level objects and directly nested objects must remain unchanged. Validate by writing a JUnit 5 test in the my-gateway test tree (using the same test framework as HybridRateLimiterFilterTest.java and RetryGlobalFilterTest.java) that asserts masking works for sensitive fields inside arrays, nested arrays of objects, and mixed structures. The test should compile and pass with mvn test -pl my-gateway -Dtest=SensitiveDataSanitizerTest executed from /app/source. No new dependencies may be added.